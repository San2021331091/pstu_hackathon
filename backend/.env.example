# PostgreSQL connection (matches docker-compose.yml)
DATABASE_URL="postgresql://money:money@localhost:5432/money_movement?schema=public"

# Auth
JWT_SECRET="change-me-in-production-please"
JWT_EXPIRES_IN="7d"

# Business rules
SIGNUP_BONUS_POISHA=10000000   # BDT 100,000 (stored as poisha; 1 taka = 100 poisha)

PORT=3001
